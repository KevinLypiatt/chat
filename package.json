{
  "name": "backend",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "build": "NODE_OPTIONS=--openssl-legacy-provider cd chat-app && npm install && npm run build",
    "build:react": "echo 'Starting React build...' && cd chat-app && npm install && npm run build && echo 'React build completed.'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "fluent-ffmpeg": "^2.1.3",
    "fs": "^0.0.1-security",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.86.2",
    "pg": "^8.13.3"
  },
  "engines": {
    "node": ">=14"
  }
}
